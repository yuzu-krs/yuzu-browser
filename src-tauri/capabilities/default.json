{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the UI webview",
  "windows": ["main"],
  "webviews": ["ui"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
